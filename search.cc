/**
 * search:
 * find
 * lower_bound
 * upper_bound
 */

int find(int *arr, int size)
{
    return -1;
}

int lower_bound(int *arr, int size)
{
    return -1;
}

int upper_bound(int *arr, int size)
{
    return -1;
}
