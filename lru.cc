/**
 * Least Recently Used cache:
 * get(key)
 * set(key, value)
 */

class LRUCache {
public:
    LRUCache(int capacity) {}
    // return -1 if not exist
    int get(int key) {}
    void set(int key, int value) {}
};
