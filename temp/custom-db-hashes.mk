# md5sum of the model DBs in the "objs/<platform>/make-library" directory
# See recipe in cov-analyze.mk
#
# Note that this hash is for linux64 models (see bug 41976).  It's impractical
# to have hashes for all platforms because that would require *updating* them
# all every time there's a change.
CUSTOM_MODELS_DB_HASH:=b7be9ae19bf0c4a58d38dfbdf4832b85
